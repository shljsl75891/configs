// Minimal stub for @earendil-works/pi-tui — used only during local test runs.
// In production pi loads extensions with its own module-resolution context that
// finds the real package; this stub is invisible to pi.
export class Editor {
	insertTextAtCursor() {}
	setText() {}
	getLines() { return []; }
}
export const Key = {};
export const matchesKey = () => false;
export const visibleWidth = (s) => s.length;
export const wrapTextWithAnsi = (s) => [s];
